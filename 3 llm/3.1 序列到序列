有一种问题是需要处理序列到序列的转换任务，比如机器翻译、文本摘要等。

这些任务的输入和输出都是序列，但它们的长度可能不同，且输出序列依赖于输入序列的内容。


根据具体的任务，可能分为以下几类：
1、相同长度的序列转换：输入和输出序列长度相同，如某些编码任务。
2、不同长度的序列转换：输入和输出序列长度不同，如机器翻译、文本摘要等。




对于这些任务，通常使用编码器-解码器（Encoder-Decoder）架构的模型，如Transformer模型。